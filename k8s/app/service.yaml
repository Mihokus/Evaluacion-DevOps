apiVersion: v1
kind: Service
metadata:
  name: catalogo-service
  namespace: catalogo
spec:
  selector:
    app: catalogo-app
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      nodePort: 30082
  type: NodePort
